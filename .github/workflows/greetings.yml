name: Greetings

on:
  pull_request_target:
  issues:

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write

    steps:
      - uses: actions/first-interaction@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

          issue-message: |
            Hi there! Thanks for opening your first issue in this repository.  
            Your contribution is appreciated — a maintainer will review it soon! 🚀

          pr-message: |
            Welcome to the project!  
            Thanks for submitting your first pull request.  
            We’re glad to have you here — someone will review it shortly.
